spring.application.name=testApp
server.port=${PORT:8080}

# ===============================
# = POSTGRESQL DATABASE CONFIGURATION
# ===============================
# Uses environment variables if available, otherwise uses default values
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://bugwe0.h.filess.io:61006/social_keeptaxarm}
spring.datasource.username=${DATABASE_USERNAME:social_keeptaxarm}
spring.datasource.password=${DATABASE_PASSWORD:767f6876cce8411d4666b3239109607e08e1ee4d}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.hikari.connection-timeout=15000
spring.datasource.hikari.maximum-pool-size=2
spring.datasource.hikari.minimum-idle=0
spring.datasource.hikari.idle-timeout=180000
spring.datasource.hikari.max-lifetime=300000
spring.datasource.hikari.leak-detection-threshold=30000
spring.datasource.hikari.connection-test-query=SELECT 1

# ===============================
# = JPA / HIBERNATE CONFIG
# ===============================
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.hbm2ddl.auto=update
spring.jpa.show-sql=${SHOW_SQL:true}
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# ===============================
# = THYMELEAF
# ===============================
spring.thymeleaf.cache=false
